<Window x:Class="WindowPilot.Controls.ReleaseZoneOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ReleaseZone"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        IsHitTestVisible="False"
        Focusable="False">

    <Grid x:Name="RootGrid">
        <!-- 红色半透明遮罩：拖拽已托管窗口至侧边栏时显示 -->
        <Border x:Name="OverlayBorder"
                Background="#22F44747"
                BorderBrush="#F44747"
                BorderThickness="2"
                CornerRadius="8"
                Opacity="0">
            <Grid>
                <TextBlock Text="拖放至此处解除托管"
                           Foreground="White"
                           FontSize="18"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Opacity="0.9">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.5" />
                    </TextBlock.Effect>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</Window>
