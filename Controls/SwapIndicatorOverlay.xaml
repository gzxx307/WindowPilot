<Window x:Class="WindowPilot.Controls.SwapIndicatorOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SwapIndicator"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        IsHitTestVisible="False"
        Focusable="False">

    <Grid x:Name="RootGrid">
        <!-- 蓝色半透明遮罩：拖拽已托管窗口经过另一个槽位时显示 -->
        <Border x:Name="OverlayBorder"
                Background="#22007ACC"
                BorderBrush="#007ACC"
                BorderThickness="3"
                CornerRadius="6"
                Opacity="0">
            <Grid>
                <TextBlock Text="⇄ 松开以互换位置"
                           Foreground="White"
                           FontSize="16"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Opacity="0.9">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.5" />
                    </TextBlock.Effect>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</Window>
